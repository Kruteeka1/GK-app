<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GK Questions</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f8ff;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }
        .container {
            text-align: center;
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
        }
        .fact-button {
            padding: 10px 20px;
            font-size: 16px;
            color: #fff;
            background-color: #007bff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }
        .fact-button:hover {
            background-color: #0056b3;
        }
        .fact-display {
            margin: 20px 0;
            font-size: 18px;
            color: #333;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>General Knowledge Quiz</h1>
        <button class="fact-button" onclick="displayQuestion()">Get GK Question</button>
        <div class="fact-display" id="factDisplay"></div>
    </div>

    <script>
        const questions = [
            {
                question: "What is the capital of France?",
                answer: "Paris"
            },
            {
                question: "Who wrote 'Hamlet'?",
                answer: "William Shakespeare"
            },
            {
                question: "What is the largest planet in our solar system?",
                answer: "Jupiter"
            },
            {
                question: "What year did the Titanic sink?",
                answer: "1912"
            },
            {
                question: "Who painted the Mona Lisa?",
                answer: "Leonardo da Vinci"
            }
        ];

        function displayQuestion() {
            const randomIndex = Math.floor(Math.random() * questions.length);
            const question = questions[randomIndex];
            document.getElementById('factDisplay').innerHTML = `<strong>Question:</strong> ${question.question}<br><strong>Answer:</strong> ${question.answer}`;
        }
    </script>
</body>
</html>

